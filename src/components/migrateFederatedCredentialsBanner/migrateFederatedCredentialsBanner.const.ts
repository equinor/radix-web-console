// TODO: #1373 - This is a temporary solution and should be removed once the migration is complete and all users have updated their configurations.

// This should have been in externalUrls.ts, but it is only temporary, so it is defined here for now.
export const migrateFederatedCredentialsGuideUrl =
  'https://www.radix.equinor.com/guides/federated-credentials-migration/'

// This should have been in use-local-storage.ts, but it is only temporary, so it is defined here for now.
export const hideMigrateFederatedCredentialsBanner = 'hideMigrateFederatedCredentialsBanner'
export const AFFECTED_CLUSTER_NAMES = ['eu-18', 'c2-11', 'weekly-32']
