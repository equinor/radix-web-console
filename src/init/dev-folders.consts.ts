/** Folders under `src/` that may contain a `dev.tsx` or `integration.tsx` file
 * for the dev sandbox routes (`/dev-component/*`, `/dev-integration/*`).
 */
export const DEV_LOOKUP_FOLDERS = ['components', 'pages']
